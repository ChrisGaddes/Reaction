<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chrisgaddes.reaction">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait"
        android:fullBackupContent="@xml/mybackupscheme">
        <activity android:name="com.chrisgaddes.reaction.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainIntroActivity"
            android:theme="@style/Theme.Intro" />


        <activity android:name=".ThirdActivity">
            android:label = "Third Activity Page"
        </activity>

        <activity android:name=".MyPreferencesActivity">
            android:label = "My Preferences"
        </activity>

    </application>

</manifest>